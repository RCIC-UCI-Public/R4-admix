
!include common.yaml
---
- package: R module MatrixModels
  name: MatrixModels
  rpkgname: MatrixModels
  version: "{{versions.MatrixModels}}"
  release: "{{versions.MatrixModels_release}}"
  baseurl: https://cran.r-project.org/src/contrib
  src_tarball: "{{rpkgname}}_{{version}}-{{release}}.{{extension}}"
  vendor_source: "{{baseurl}}/{{rpkgname}}_{{version}}-{{release}}.{{extension}}"
  requires:
    - R_{{Rversion}}
