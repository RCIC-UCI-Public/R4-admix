
!include common.yaml
---
- package: R module data_tree
  name: data_tree
  rpkgname: data.tree
  version: "{{versions.data_tree}}"
  baseurl: https://cran.r-project.org/src/contrib
  src_tarball: "{{rpkgname}}_{{version}}.{{extension}}"
  vendor_source: "{{baseurl}}/{{rpkgname}}_{{version}}.{{extension}}"
  requires:
    - R_{{Rversion}}
    - R_{{Rversion}}-R6
    - R_{{Rversion}}-stringi
