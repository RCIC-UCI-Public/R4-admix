
!include common.yaml
---
- package: R module multcompView
  name: multcompView
  rpkgname: multcompView
  version: "{{versions.multcompView}}"
  release: "{{versions.multcompView_release}}"
  baseurl: https://cran.r-project.org/src/contrib
  src_tarball: "{{rpkgname}}_{{version}}-{{release}}.{{extension}}"
  vendor_source: "{{baseurl}}/{{rpkgname}}_{{version}}-{{release}}.{{extension}}"
  requires:
    - R_{{Rversion}}
