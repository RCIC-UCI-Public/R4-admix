# Builds the module for R. 
!include R.yaml
!include rcic-module.yaml
---
- package: R Module
  category: LANGUAGES
  module:
    lib: lib64
    setenv:
      - *Setenv
    prereq:
      - "{{build.modules}}"
    requires:
      - *Requires
      - "{{module.prereq}}"
